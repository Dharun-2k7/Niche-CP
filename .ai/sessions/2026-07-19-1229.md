# Session Log

**Date:** 2026-07-19

## Objectives
*   Implement NicheCP UI/UX improvements (global nav, hero redesign).
*   Address technical debt (Solved Count sync bug).
*   Enforce security (multi-step email update, 24-hr password reset cooldown).

## Completed Actions
*   Added `app.js` to `contests.html` to standardize navigation.
*   Updated Hero Text and Call-to-Actions in `index.html`.
*   Restricted Admin panel to `dharunkaarthick07@gmail.com` in `app.js`.
*   Removed unused icons and added a Logout link in `app.js`.
*   Created `favicon.svg` and added to main pages.
*   Altered PostgreSQL `users` table to add `last_email_change TIMESTAMP`.
*   Fixed Solved Count bug in `profile.go` using `COUNT(DISTINCT problem_id)`.
*   Implemented two-step OTP flow for email changes in `profile.go` and `main.go`.
*   Added 24-hour password reset cooldown in `auth_flows.go`.

## Open Questions / Notes
*   None.
